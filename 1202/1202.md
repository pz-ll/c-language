# 运算 2

- `算数运算`
- `赋值`
- `关系运算`
- `逻辑运算`
- `位运算`
- `三元运算`

## 流程控制

- `顺序结构`
- `分支结构`（选择结构）
- `循环结构`

### 分支语句
- `if ()`
- `if () else`
- `if () else if () else if () else`
- `switch() {case }`

```c
    // 条件
    int a;
    if (7 == a) // 1;

    if ()
        // 1;

    if ()
    {
        // 1
        // 2
    }
    // if - else 两个代码块
    if ()
    {
        // 1
    }
    else
    {
        // 2
    }

    // 3 多路分支
    if (1)
    {
        // 1
    }
    else if (2)
    {
        // 2
    }
    else if (3)
    {
        // 3
    }
    else
    {
        // 4
    }

```

## 关系运算（比较运算）

结果：真 1、假 0

- `==`
- `!=`
- `>`
- `>=`
- `<`
- `<=`


## 逻辑运算

- `&&` 与，左右都真 真，有一个假 假，短路
- `||` 或，左右有一个真，就是真
- `!` 非，真变假，假变真

```c

    int age;
    // if (18 <= age <= 70)
    if (18 <= age && age < 70)
    {
        //
    }
    else
    {

    }


```


## 作业
1. 输入 x, y, z
    输出 max, mid, min

2. 阅读 ch5, 课后部分练习题