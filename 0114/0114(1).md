# 宏和预处理


宏：文本替换

场景：
- 定义常量
- 定义函数
- 导入头文件
- 条件编译

预处理器：一个程序，早期独立的，现在编译器包含

开发流程
- 预处理（预处理器）
- 编译（编译器）
- 链接（编译器）
- 执行

## 宏定义
- 无参数宏：常量
- 带参数宏：替代函数，优点：快，缺点：源代码膨胀
- `#` 转为字符串，`##` 拼接字符串

```c
// 宏
#include <stdio.h>

// 宏名称
#define PI 3.1415
#define C(r) 2*PI*(r)
#define MUL(a,b) (a)*(b)
// 参数无类型：解决函数不能重载的问题
#define MAX(a,b) (a)>(b)?(a):(b)
// # 转字符串
// #define STR(x) #x
#define TO_STR(x) #x
#define STR(x) TO_STR(x)
// ## 拼接字符串a,b
#define CAT(a,b) a##b

// 函数**重载**
// char max(char a, char b)
// {
//     return a > b ? a : b;
// }
// int max(int a, int b)
// {
//     return a > b ? a : b;
// }
// double max(double a, double b)
// {
//     return a > b ? a : b;
// }

double c1(int r)
{
    return 2 * r * PI;
}

int main(int argc, char const *argv[])
{
    int num1 = 100;
    int num2 = 200;
    int num3 = 300;
    printf("%d\n", CAT(num, 1));
    printf("%d\n", CAT(num, 2));
    printf("%d\n", CAT(num, 3));

    

    // int a = 2;
    // int b = 3;
    // // printf("%d+%d=%d\n", a, b, a+b);
    // printf("%s=%d\n", STR(a+b), a+b);
    // printf("%s\n", STR(PI));

    // printf("%d\n", MAX(2, 3));
    // printf("%f\n", MAX(123.89, 89.5));
    // printf("%c\n", MAX('Z', 'b'));      // char,short -> int
    // 2*PI*R
    // int r = 5;
    // printf("%f\n", 2*PI*r);        // 表达式
    // printf("%f\n", C(r+1));       // 宏
    // printf("%f\n", c1(r+1));      // 函数
    // printf("%d\n", MUL(2, 3));       // 6
    // printf("%d\n", MUL(2+1, 3+2));   // 3*5
    // printf("%d\n", (2+1)*(3+2));   // 3*5
    return 0;
}

```

## include

// 尖括号：直接在系统的加载路径 /usr/include
#include <stdio.h>

// 双引号：先搜索当前路径，再搜索系统路径
#include "my.h"


```c
// 避免重复包含
// my.h         __MY_H
// my_math.h    __MY_MATH_H

// 若未定义 IF Not Define
#ifndef __MY_H
// 则定义，确保定义包含一次
#define  __MY_H

// 定义常量（宏）
#define N 5
// 函数声明
int my_max(int, int);
int my_min(int, int);

#endif

```