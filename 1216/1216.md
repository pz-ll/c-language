# 位运算 & 字符

## 位运算

数值转化为二进制逐位进行运算，结果数值

- `&`   与
- `|`   或
- `^`   异或
- `~`   取反
- `>>`  右移
- `<<`  左移

### & 与

两个1得1，其他得0，掩码运算

```c
    int a = 2;
    int b = 3;
    // 1010101000   x
    // 0000000100   4
    // 0000000000

    if (a % 2 == 1) // % 除法取余数
    if (a & 1 == 1) // 
    // 1010101x
    // 00000001
    // 0000000x
```

IP      192.168.30.133
mask    255.255.255.0
------------------------
        192.168.30.0

## | 或

只要一个1就得1，两个0得0

    1100
    1010
    ----
    1110

通知
- 响铃  1       0001    t1
- 震动  2       0010    t2
- LED  4       0100     t3

    int v = 0;
    v = v | t1;
    v = v | t2;


    int v = 6       // 0110

    if (v&t1 == t1)
    {
        // 响铃
        0110
        0001
        ----
        0000
    }

    if (v&t2 == t2)
    {
        // 震动
    }

## ^ 异或

一样得0，不一样得1

    1100
    1010
    ----
    0110

```c
    int a[] = {12, 13, 42, 13, 15, 12, 15};
    
    int n = 0;
    for (int i = 0; i < 7; i++)
    {
        n = n ^ a[i];
    }
    printf("%d\n", n);
    return 0;
```

## ~

一元运算，按位取反

```c
    // 00000000001
    // 10000000001  -1
    // 负数：按位取反，再加一
    int a = 3;      //  0000 0011
    int b = ~a;     //  1111 1100 = -4

    printf("%d\n", b);

    printf("%032b\n", a);
    printf("%b\n", b);

    printf("%d,%d\n", 13, ~13);
    printf("%d,%d\n", -13, ~(-13));
    printf("%d,%d\n", 0, ~0);
```

## >> 位移

<<  左移动，在原数值右侧补 0，乘以 2 的 n 次方
>>  右移动，低位截断，左则补 0，除以 2 的 n 次方

4 << 3
    0100000

15 >> 3         15 / 8 = 1
    0001

```c
    int n;
    printf("请输入整数：");
    scanf("%d", &n);

    // 方向反了
    // while (n > 0)
    // {
    //     printf("%d", n % 2);
    //     n /= 2;
    // }
    for (int i = 0; i < 32; i++)
    {
        // 方案一
        // printf("%d", (n >> (31 - i)) & 1);

        // 方案二：构造一个 2^n
        int b = 1 << (31 - i);
        printf("%d", (n & b) == b); // 1,0
        if (i%8 == 7) printf(" ");
    }

```