# 字符串

## 定义

```c
    char s1[16] = "abc";  // sizeof=16 strlen=3
    // s1 = "xyz";
    s1[0] = 'x';

    char *s2 = "abc";     // sizeof=8 strlen=3
    s2 = "xyz";
```
## 函数

在标准库 string.h 中定义字符串相关的函数
- `strlen`  长度
- `strcpy`  复制
- `strcat`  拼接
- `strcmp`  比较
- `strchr`  查找字符
- `strstr`  查找字串
- `strtok`  差分字符串
- `strnxxx` n范围安全的, 多一个参数
- `memcpy`  内存拷贝
- `memmove` 内存移动
- `memset`  内存填充
- `memcmp`

### 终端读取字符串
- scanf()
- gets(s)
- fgets(s, size, stdin)

```javascript
    // JSON
    // {} 数据对象，结构体，键值对，字典
    // [] 数组
    {
        name: "alice",
        age: 23,
        tel: [131, 186],
        address: {
            city: "",
            street: ""
        }
    }
```

## 应用

1.定义一个函数，传入整数，获得整数转16进制

    char x[] = "0123456789ABCDEF";
    x[i]

0~9 A~F

31  0x1F
1   15
0   1

2.把 12345 转汉字大写形式


3.大数值的加减乘除
    char a[32];
    char b[32];

    a + b
    a - b

    a * b
            补零，加法

    a / b
    1333333333333333333
            33333333333