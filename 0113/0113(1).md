# 结构体数组、指针

## 结构体数组

```c
    // 定义常量，宏定义
#define NAME_SIZE 10

// 定义一个新的数据类型
typedef struct Student
{
    char name[NAME_SIZE];   // 姓名（成员、字段）
    int age;                // 年龄
} Student;


    Student a[4] = {
        {"吕布", 21}, 
        {"赵子龙", 19}, 
        {.age=23, .name="貂蝉"}
    };
    strncpy(a[3].name, "曹操", NAME_SIZE-1);
    a[3].age = 24;

    // 指针变量
    Student *p = a;
    for (int i = 0; i < 4; i++)
    {
        // printf("%s\t%d\n", a[i].name, a[i].age);     // 数组下标
        // printf("%s\t%d\n", (*p).name, (*p).age);     // 指针
        printf("%s\t%d\n", p->name, p->age);            // 指针 - >
        p++;
    }
```

## 结构体与动态内存管理

```c
    // 动态（长度可变）数组
    Student *ptr = (Student *) malloc(4 * sizeof(struct Student));
    if (!ptr)
    {
        printf("内存分配失败\n");
        return EXIT_FAILURE;
    }
    Student *p = ptr;
    for (int i = 0; i < 4; i++)
    {
        printf("请输入姓名与年龄：");
        scanf("%s %d", p->name, &p->age);
        p++;
        // 字符串：学生0001,学生0002
        // char str[16];
        // sprintf(str, "学生%04d", i+1);
        // 字段赋值
        // strcpy((ptr + i)->name, str);
        // (ptr + i)->age = 18 + i;
        // strcpy(ptr[i].name, str);
        // ptr[i].age = 18 + i;
        // 打印
    }

    for (int i = 0; i < 4; i++)
    {
        printf("%s\t%d\n", (ptr+i)->name, (ptr+i)->age);
    }


```


```c
struct Date
{
    int year;
    int month;
    int day;
};

typedef struct Person
{
    char name[16];          // 姓名
    int age;                // 年龄
    struct Date birthday;   // 生日
    struct Person *lover;   // 配偶 指针
} Person;


int main(int argc, char const *argv[])
{
    Person p2 = {"bob"};
    Person p1 = {"alice", 21, {2004,1,13}, &p2};
    // p1.lover = &p2;

    printf("%s\t%d\t%d-%d-%d\t%s\n", p1.name, p1.age, 
        p1.birthday.year, 
        p1.birthday.month, 
        p1.birthday.day,
        p1.lover->name
    );

    return 0;
}

```

## 作业

1. 品牌、产品

品牌
- 名称
- 创建时间
- 产品列表

产品
- 名称
- 价格
- 品牌（结构体、指针？）


