# 函数

函数是构成 C 语言的基本单元
函数是命名的代码块、可复用的功能单元
函数是进程模块化开发的基础

编程范式
- 脚本编程
- 函数：面向过程编程
- 类：面向对象编程

```js
    // python、perl、js
    // 脚本编程
    let uname = "tom";
    let age = 22;

    console.log(`Hello, I'm ${uname}, ${age} years old.`);

    //python
    print("Hello Wolrd");
```

```c
    int main()
    {
        //
    }
    // main    
```

```java

    // 类
    class App
    {
        //
        foo() {}

        // 函数
        main() {
            System.out.print("Hello");
        }
    }

    //
    App.main()

```

## 定义与调用

```c
bool is_prime(int n)
{
    // 标识符
    bool flag = true;
    // 2~n的平方根
    for (int i = 2; i*i <= n; i++)
    {
       if (n%i == 0)
       {
            flag = false;
            break;
       }
    }
    return flag;
}
```

## 递归

一个函数中调用自身
- 定义一个收敛条件，结束递归，否则内存不足，爆栈

1. 阶乘

    n! = 1 * 2 * 3 *... * n
    n! = n * (n-1)!

## 作业
1. 斐波那契数列，循环和递归两种方案
2. gcd 最大公约数：递归
3. 